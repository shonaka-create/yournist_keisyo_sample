document.addEventListener('DOMContentLoaded',()=>{const list=document.querySelector('[data-business-tabs]'),tablist=document.querySelector('.r-business-tabs');if(!list||!tablist)return;const tabs=[...tablist.querySelectorAll('[role="tab"]')],panels=[...list.querySelectorAll('[role="tabpanel"]')];if(tabs.length!==panels.length)return;const activate=(index,focus)=>{tabs.forEach((tab,i)=>{const on=i===index;tab.classList.toggle('is-active',on);tab.setAttribute('aria-selected',String(on));tab.tabIndex=on?0:-1;panels[i].hidden=!on});if(focus)tabs[index].focus()};tabs.forEach((tab,i)=>{tab.addEventListener('click',()=>activate(i,false));tab.addEventListener('keydown',e=>{const step=e.key==='ArrowRight'?1:e.key==='ArrowLeft'?-1:0;if(!step)return;e.preventDefault();activate((i+step+tabs.length)%tabs.length,true)})});activate(0,false)});
