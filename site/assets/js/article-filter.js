document.addEventListener('DOMContentLoaded',()=>{const wrap=document.querySelector('[data-article-list]');if(!wrap)return;const cards=[...wrap.querySelectorAll('[data-category]')],buttons=[...document.querySelectorAll('[data-filter]')],count=document.querySelector('[data-result-count]');const apply=value=>{let visible=0;cards.forEach(card=>{const show=value==='all'||card.dataset.category===value;card.hidden=!show;if(show)visible++});buttons.forEach(btn=>btn.setAttribute('aria-pressed',String(btn.dataset.filter===value)));if(count)count.textContent=`${visible}件の記事`};buttons.forEach(btn=>btn.addEventListener('click',()=>apply(btn.dataset.filter)));apply('all')});
